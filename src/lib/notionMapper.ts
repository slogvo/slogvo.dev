// import { blockMapper, type NotionBlock } from 'react-notion-blocks';

// export const renderNotionContent = (blocks: NotionBlock[]) => {
//   return blocks.map((block) => blockMapper(block));
// };
